# Server Configuration
NODE_ENV=development
SERVER_PORT=3000
ENV=dev

# MongoDB Atlas Configuration
# Get your connection string from MongoDB Atlas:
# 1. Go to https://cloud.mongodb.com/
# 2. Click "Connect" on your cluster
# 3. Choose "Connect your application"
# 4. Copy the connection string and replace <password> with your database password
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/navidad-reviews?retryWrites=true&w=majority

# Example for local MongoDB (if not using Atlas):
# MONGODB_URI=mongodb://localhost:27017/navidad-reviews

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-token-secret-change-in-production
JWT_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# Frontend Configuration
FE_URI=http://localhost:5173
VITE_API_URL=http://localhost:3000/api

# Google OAuth (Optional)
VITE_GOOGLE_CLIENT_ID=your-google-client-id

# Production Configuration
# For production, use the same MONGODB_URI but ensure it's from a production cluster
# FE_URI=https://your-domain.com
# VITE_API_URL=https://your-domain.com/api
